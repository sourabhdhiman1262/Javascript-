let Name = new String("Sourabh ");
let repoCount = 15;

// ---------- old way to add string    ----------

// console.log(Name + repoCount);

// this syntex is old don't use that insted use moden syntex like,

// ---------- Mordon way ---------

// console.log(`My Name is ${} and my repocount is ${}`);

// in which we use backticks `` using backticks is called string interpoltion in which we creating place holders ${}  so that we can add simple or even perform any operation like charactart, indexof ex. 

console.log(`My Name is ${Name} and my repocount is ${repoCount}`);